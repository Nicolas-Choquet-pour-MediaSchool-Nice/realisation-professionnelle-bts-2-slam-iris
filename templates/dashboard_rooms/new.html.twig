{% extends 'base.html.twig' %}

{% block title %}Créer une salle{% endblock %}

{% block body %}
<div class="container">
    <div class="header-section">
        <h1>Créer une salle</h1>
        <a href="{{ path('app_dashboard_rooms') }}" class="btn btn-outline">Retour à la liste</a>
    </div>

    <div class="card">
        {{ include('dashboard_rooms/_form.html.twig', {'button_label': 'Créer'}) }}
    </div>
</div>
{% endblock %}
