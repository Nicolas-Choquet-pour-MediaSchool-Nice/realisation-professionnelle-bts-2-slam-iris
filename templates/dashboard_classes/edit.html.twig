{% extends 'base.html.twig' %}

{% block title %}Modifier la classe{% endblock %}

{% block body %}
<div class="container">
    <div class="header-section">
        <h1>Modifier la classe : {{ school_class.name }}</h1>
        <a href="{{ path('app_dashboard_classes') }}" class="btn btn-outline">Retour à la liste</a>
    </div>

    <div class="card">
        {{ include('dashboard_classes/_form.html.twig') }}

        <div style="margin-top: 20px; padding-top: 20px; border-top: 1px solid #eee; display: flex; justify-content: flex-end;">
            {{ include('dashboard_classes/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
