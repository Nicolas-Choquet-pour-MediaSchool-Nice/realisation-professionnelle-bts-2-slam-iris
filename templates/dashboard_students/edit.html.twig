{% extends 'base.html.twig' %}

{% block title %}Modifier l'étudiant{% endblock %}

{% block body %}
<div class="container">
    <div class="header-section">
        <h1>Modifier l'étudiant : {{ student.user.firstname }} {{ student.user.lastname }}</h1>
        <a href="{{ path('app_dashboard_students') }}" class="btn btn-outline">Retour à la liste</a>
    </div>

    <div class="card">
        {{ include('dashboard_students/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div style="margin-top: 20px; padding-top: 20px; border-top: 1px solid #eee; display: flex; justify-content: flex-end;">
            {{ include('dashboard_students/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
