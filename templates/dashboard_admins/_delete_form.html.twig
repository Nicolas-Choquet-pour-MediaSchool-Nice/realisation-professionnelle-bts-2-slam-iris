<form method="post" action="{{ path('app_dashboard_admins_delete', {'id': admin.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet administrateur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ admin.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
